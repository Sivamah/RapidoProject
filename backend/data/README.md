# FastAPI + React unified mobility/delivery platform (backend data artifacts)

New application data (runtime data, DB dumps, imported fixtures) that is not a
source dataset goes here during active development. Source datasets continue to
live in `backend/datasets/` (kept unchanged).
